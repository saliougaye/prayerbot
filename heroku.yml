build:
  docker:
    botworker: telegrambot/Dockerfile
    worker: workers/Dockerfile
  config:
    NODE_ENV: $NODE_ENV
    REDIS: $REDIS_URL
    BOT_TOKEN: $BOT_TOKEN
    API: $API

run:
  botworker: npm start
  worker: npm start